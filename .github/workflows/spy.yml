name: Spy Single Video (Final)

on:
  workflow_dispatch:

jobs:
  spy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Setup
        uses: actions/checkout@v3

      - name: Install Powerful Tool (yt-dlp)
        # Hum Python script nahi, seedha yt-dlp tool install karenge
        run: |
          sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp

      - name: Jasoosi Shuru (Android Mode)
        # 👇 Yeh command YouTube ko lagega ki Android Phone se aayi hai
        run: |
          echo "--------------------------------------"
          echo "🕵️‍♂️ Analyzing Video..."
          echo "--------------------------------------"
          
          yt-dlp "https://youtube.com/shorts/jtU7-Vnl5Ls" \
          --print "🎥 TITLE: %(title)s" \
          --print "📂 CATEGORY: %(categories)s" \
          --print "🏷️  TAGS: %(tags)s" \
          --extractor-args "youtube:player_client=android" \
          --no-warnings \
          --ignore-errors
