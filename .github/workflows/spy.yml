name: Run Competitor Spy

on:
  workflow_dispatch:

jobs:
  spy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Python Setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Libraries Install
        # 👇 Sirf 'requests' install hoga (No Login tools)
        run: pip install requests

      - name: Jasoosi Shuru Karo
        # 👇 Secrets ki zaroorat nahi hai
        run: python spy.py
